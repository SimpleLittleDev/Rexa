# Rexa Dashboard

Phase 4 target: React/Vite operations dashboard with chat UI, task log, browser screenshot viewer, tool call viewer, memory viewer, sub-agent status, provider status, and config viewer.

The current MVP exposes REST via:

```bash
npm run api
```

Health endpoint:

```bash
curl http://127.0.0.1:8786/health
```
